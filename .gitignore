dist/
tests/build/
# Reference frames for the release parity gate. Large, and not ours to
# redistribute - they are somebody's actual sky. Never commit them.
testset/
*.xssk
*.log
.DS_Store
*:Zone.Identifier
# Local working files (screenshots, run artifacts dropped at the repo root)
/*.png
/*.jpg
